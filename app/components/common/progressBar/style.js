import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  bar: {
    height: 6,
    borderRadius: 3,
    overflow: 'hidden',
  },

  dynamic: {
    height: 6,
  },

  view: {
    height: 28
  },

  title: {
    fontSize: 12,
    alignSelf: 'flex-end',
    marginBottom: 10
  }
});
